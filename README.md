# Chatbot Kesehatan Mental

## Instalasi
1. Clone repo
2. Buat virtual env: `python -m venv venv`
3. Aktifkan: `source venv/bin/activate` (atau `.\\venv\\Scripts\\activate` di Windows)
4. Install: `pip install -r requirements.txt`
5. Buat `.env` di root:
